<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace ="Feed">
   <select id="selectAll" resultType="kh.init.feeds.FeedDTO">
      select * from tb_feed
   </select>

	<select id="getFeedSeq" resultType="integer">
		select feed_seq.nextval from dual
	</select>

   <insert id="registerFeed" parameterType="kh.init.feeds.FeedDTO">      
      insert into tb_feed values(#{feed_seq},#{email},#{nickname},#{title},#{contents},#{relation},sysdate) 
   </insert>
   
   <insert id="registerMedia" parameterType="java.util.Map">
   	  insert into tb_feed_file values(#{feed_seq}, feed_file_seq.nextval, #{media})
   </insert>
   
   <delete id="deleteFeed" parameterType="java.util.Map">
      delete from tb_feed where feed_seq = #{seq}
   </delete>
   
   <select id="detailView" resultType="kh.init.feeds.FeedDTO">
      select * from tb_feed where feed_seq = #{feed_seq}
   </select>
   
   <select id="getMediaList" resultType="String">
	  select media from tb_feed_file where feed_seq = #{feed_seq}
   </select>
</mapper>